> *This was generated by AI during triage.*

# ISSUE-061: Rebrand Tokenix → Tokenizer

## Labels

- `type: refactor`
- `priority: P1`
- `state: ready-for-agent`
- `sprint: 02`

## Mål

Byt namn på produkten och Go-modulen från `tokenix` till `tokenizer` överallt där det syftar på produkten/repot. GitHub-repot är redan omdöpt till `magnusfroste/tokenizer`.

## Scope

Träffytan är 14 filer (verifierad med `grep -rli "tokenix" . --exclude-dir=.git`):

- `go.mod` — module-path
- 11 `.go`-filer under `cmd/` och `internal/` — alla import-paths som börjar med `github.com/magnusfroste/tokenix/`
- `CLAUDE.md` — produktnamn i prosa
- `DECISION_LOG.md` — historiska referenser
- `docker-compose.yml` — service-/container-/volym-namn om de innehåller `tokenix`

Utöver filerna: `git remote set-url origin https://github.com/magnusfroste/tokenizer.git`.

## Inte i scope (viktigt)

- **`internal/openai/`** byts INTE. Det refererar till OpenAI-API-protokollet (wire format), inte produktnamnet.
- Docs under `00-product/`, `01-architecture/`, `02-adr/`, `03-backlog/`, `04-sprints/`, `05-issues/`, `06-engineering/`, `07-operations/`, `08-templates/` namnger inte produkten — lämna orörda om de inte träffas av grep.
- Inga semantiska ändringar i kod, bara identifierare/strängar.

## Acceptanskriterier

- `grep -rli "tokenix" . --exclude-dir=.git` returnerar 0 träffar (case-insensitive).
- `go.mod` har `module github.com/magnusfroste/tokenizer`.
- `go build ./...` grön.
- `go test ./...` grön (alla befintliga tester passerar utan ändrad logik).
- `go vet ./...` ren.
- `git remote -v` pekar på `magnusfroste/tokenizer`.
- `internal/openai/`-paketet och dess imports orörda.

## Tekniska noter

- Använd `go mod edit -module github.com/magnusfroste/tokenizer` följt av en sökning-och-ersätt över `.go`-filerna (`gofmt -r` eller enkel `sed` på imports — verifiera med `goimports` efteråt).
- Case matters: `Tokenix` i prosa → `Tokenizer`; `tokenix` i identifierare/paths → `tokenizer`.
- En commit för helheten är OK (mekanisk rename). Commit-message: `refactor: rename tokenix → tokenizer (module path + product name)`.
- Uppdatera inte CHANGELOG/RELEASE-noter — finns inte ännu i sprint 1.

## Klar när

- Acceptanskriterierna är uppfyllda.
- En ren commit är pushad till `main`.
- `make dev` startar utan att klaga på module-path.
